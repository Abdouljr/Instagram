import 'package:flutter/material.dart';

class Postwidget extends StatelessWidget {
  const Postwidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 1000,
      color: const Color.fromARGB(255, 226, 234, 226),
    );
  }
}
